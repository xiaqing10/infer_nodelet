# infer_nodelet


#### 1. 推理后端解耦 (IDetector 接口)
- 新增 `include/i_detector.h`：纯虚接口 `IDetector`，定义 `init()` / `detect()` / `destroy()` 标准接口
- 新增 `include/cambricon_detector.h` + `src/cambricon_detector.cpp`：`CambriconDetector` 继承 `IDetector`，封装寒武纪 MLU 推理
- CMakeLists.txt 支持条件编译 `USE_CAMBRICON` / `USE_SOPHON` / `USE_RKNN`（互斥），方便切换推理后端
- 后端扩展方式：新建 `include/sophon_detector.h` + `src/sophon_detector.cpp` 实现 `IDetector` 接口即可

#### 2. 交通事件分析 (TrafficAnalyzer)
- 新增 `src/traffic_analyzer.h` + `src/traffic_analyzer.cpp`：交通事件分析引擎
- 支持 **逆行检测** (wrong-way)：基于车道方向模板匹配
- 支持 **静止检测** (stationary)：基于帧间位移阈值
- 支持 **模板自动学习**：启动时加载已有模板，不存在则从昨日轨迹自动学习
- 每个视角（camera_type + camera_direction）独立维护模板
- 事件保存为 json + jpg，图片上绘制轨迹
- 轨迹拼接图用缩小图像缓存生成，不保存原图
- 可通过配置开关启用/禁用，禁用时完全静默（不分析、不打印日志）

#### 3. 轨迹按小时分目录保存
- 轨迹文件按 `YYYY-MM-DD/tracks/HH/` 分 24 个子目录保存
- 减少单目录文件数量，提升文件系统性能

#### 4. 双卡自动分配
- 自动检测 MLU 设备数量
- 多摄像头时轮询分配到不同设备，均衡负载

#### 5. 日志时间戳可读化
- 新增 `include/log_macros.h`：统一日志宏
- `LOG_INFO` / `LOG_WARN` / `LOG_ERROR` / `LOG_COUT` 宏
- 输出格式：`[YYYY-MM-DD HH:MM:SS] [LEVEL] message`
- `LOG_INFO` / `LOG_COUT` → stdout（infer.log）
- `LOG_WARN` / `LOG_ERROR` → stderr（infer.err）
- 替换原有 `ROS_INFO` / `ROS_WARN` / `ROS_ERROR` / `std::cout` / `std::cerr`

#### 6. 摄像头离线时长监控
- 摄像头无数据时记录离线起始时间
- 每 ~40s（600帧）打印一次累计离线时长
- 日志格式：`[WARN] No Camera Data front_main, offline for 42s`
- 数据恢复后自动重置计时

### 配置说明 (config/infer.yaml)

#### 全局参数

```yaml
det_engine_name: /path/to/model.cambricon     # 检测模型
abandon_engine_name: /path/to/abandon.cambricon  # 抛洒物检测模型
vechile_color_rate: 12   # 车型检测频率（每 N 帧检测一次）
abandon_rate: 8           # 抛洒物检测频率（每 N 帧检测一次）
min_points_len: 30        # 轨迹保存的最小长度
write_flag: true          # 是否保存轨迹结果到文件
write_path: /data/...     # 轨迹结果保存路径
byte_track_config_file: /path/to/bytetrack.yaml  # ByteTrack 配置文件
```

#### 交通事件分析

```yaml
traffic_analyzer:
  enabled: false                    # true 启用事件分析，false 完全禁用（默认）
  templates_path: /data/.../templates  # 模板文件保存路径
  events_path: /data/.../events        # 事件文件保存路径
  stationary_frames: 30                # 静止判定帧数
  stationary_threshold: 0.005          # 静止判定位移阈值（归一化坐标）
```

#### 多路摄像头可视化控制 (camera_visual)

非 test 模式下，通过 camera_visual 列表控制哪些 camera 发布图像和绘制结果。
每路 camera 由 direction（upstream/downstream）和 focal_type（long/short）唯一标识。

空列表：所有路都不发布图像、不绘制，性能最大化
```yaml
camera_visual: []
```

只开启 upstream/short 这一路的可视化：
```yaml
camera_visual:
  - {direction: upstream, focal_type: short, publish_img: true, draw_tracker: true}
```

同时开启多路：
```yaml
camera_visual:
  - {direction: upstream, focal_type: short, publish_img: true, draw_tracker: true}
  - {direction: upstream, focal_type: long, publish_img: true, draw_tracker: true}
```

每个条目字段说明：
- direction: camera 方向，upstream 或 downstream
- focal_type: 焦距类型，long 或 short
- publish_img: true 发布检测结果图像 topic，false 跳过
- draw_tracker: true 在图像上绘制检测框和轨迹，false 跳过

未在 camera_visual 中匹配到的 camera，publish_img 和 draw_tracker 均为 false。

#### Test 模式配置

```yaml
test:
  test_flag: false                    # true 启用测试模式
  camera_type: "short"
  camera_direction: "upstream"
  publish_img_topic: "/test_img"      # 图像发布 topic
  publish_img_result: "/test_ret"     # 检测结果发布 topic
  publish_fps: "/test_fps"            # FPS 发布 topic
  publish_img: true                   # 是否发布图像（false 可提升帧率）
  draw_tracker: true                  # 是否绘制检测框和轨迹（false 可提升帧率）
  receive_radar_topic: "/test_radar"  # 雷达数据 topic
  # 输入源（二选一）:
  #   1) video_path: mp4 文件路径
  #   2) receive_img_topic: ROS topic 名称（rosbag 回放用）
  video_path: ""
  receive_img_topic: "/test_img_raw"
```

### 编译 & 打包

```bash
source /opt/ros/noetic/setup.bash
cd /home/files/catkin_ws
catkin_make --only-pkg-with-deps infer_nodelet
catkin_make install   # 生成 deb 包
```


---

## 各平台处理流程

CMakeLists 通过互斥宏选择后端：`USE_CAMBRICON` / `USE_SOPHON` / `USE_ASCEND` / `USE_NVIDIA` / `USE_RKNN`（五选一必须 ON）。推理架构分两类：

- **批量流水线（Sophon / RKNN / NVIDIA ）**：全局共享一个推理管线 + 共享 worker，所有路相机共用，模型只加载一次。
- **同步单路（NVIDIA / Cambricon / Ascend）**：每路相机独立的 `Detector`（含 `abandon_detector`），在 `processRadarCamera` 中同步推理。

### 总览

| 平台 | 检测架构 | 模型加载 | 颜色分类 | 抛洒物 | SHM |
|---|---|---|---|---|---|
| Sophon | 全局三段式批量 | 全局共享 `g_pipeline`/`g_color_model`/`g_abandon_model` | 有（全局共享 worker） | 全局共享 worker | 支持 |
| RKNN | 全局多核并行（3 NPU 核） | 全局共享 `g_pipeline` | 无 | 每路 | 支持 |
| NVIDIA | 每路同步 | 每路独立 `Detector`/`VehicleColorDetector` | 有 | 每路跳帧 | 配置支持但无读取线程* |
| Cambricon | 每路同步 | 每路独立 `Detector` | 无 | 每路跳帧 | 配置支持但无读取线程* |
| Ascend | 每路同步 | 每路独立 `Detector` | 无 | 每路跳帧 | 配置支持但无读取线程* |

> \* SHM 说明：`use_shm` 配置与 `shm_reader` 初始化对所有平台生效（`infer_auxiliary.cpp:82`）。但 SHM **读取线程**仅在 `#if USE_RKNN / #elif USE_SOPHON / #elif USE_NVIDIA`（仅 `batch_mode=true` 时）分支实现（`infer_processing.cpp:281-380`）。Cambricon/Ascend 与 NVIDIA 同步模式若设 `use_shm=true`，主循环会走 `usleep + continue` 空转（`infer_processing.cpp:380`）却无任何线程推入帧数据，导致没有图像。因此实际支持 SHM 的平台：Sophon / RKNN / NVIDIA（批量模式）。

### Sophon (USE_SOPHON) —— 三段式全局批量流水线

- **架构**：**强制全局批量流水线**（无每路同步、无单帧推理路径）。所有路相机共享同一批处理 pipeline，仅存在批量模式。
- **batch 大小不可配置**：`batch_size` 从 bmodel 静态输入形状读取（`detector_sophon.cpp:510`，`dims[0]`），需重新编译 bmodel 才能改。
1. **全局模型初始化一次**（`infer_nodelet.cpp:597-630`）：`SophonPipeline`（检测，`conf/nms` 经 `setDetectThresholds` 生效）+ `RESNET g_color_model`（颜色）+ `Detector g_abandon_model`（抛洒物），所有路相机共享；结果队列 `g_result_queues`、`g_color_result_queues`、`g_abandon_result_queues` 每路各一。
2. **批量预处理线程** `batch_preprocess_thread`（`infer_pipeline_threads.cpp:48`）：round-robin 扫描各相机 `g_cam_frame_queues`，每路最多取 1 帧、多次扫描凑满 `max_full`（上限 `batch_size`；未配 `max_full` 时取相机数）→ `g_pipeline->preprocessAndForward`（`toBMI` → `pre_process` → `forward`，`sophon_pipeline.cpp:9`）→ 结果推入 `g_post_queue`。
3. **批量后处理线程** `batch_postprocess_thread`（`infer_pipeline_threads.cpp:281`）：`g_pipeline->postProcess`（`sophon_pipeline.cpp:61`）→ 按 camera_id 路由到各自 `g_result_queues`。
4. **每路 `processResult`**（`infer_processing.cpp:776`）：消费 `g_result_queues[camera_id_]` → ByteTrack 跟踪 → 雷达数据 → 颜色（`class_id>2 && area>2000` 时 crop → `g_color_queue` → 全局 `color_infer_thread` → `g_color_result_queues`）→ 抛洒物（`abandon_rate` 跳帧 → `g_abandon_queue` → 全局 `abandon_infer_thread` → `g_abandon_result_queues`）→ 发布。
5. **SHM 输入**：独立线程读共享内存 → 推 `g_cam_frame_queues`（`infer_processing.cpp:354`）。

### RKNN (USE_RKNN) —— 全局多核并行

- **架构**：全局 pipeline 内含 `RKNN_NUM_CORES = 3` 个 `YoloV8_det`，各绑定一个 NPU 核（`RKNN_NPU_CORE_0/1/2`，`rknn_pipeline.cpp:4-8`）。3 个推理线程共享同一输入队列并行消费。
1. **全局模型初始化一次**（`infer_nodelet.cpp:631-653`）：`RknnPipeline`（检测，`conf/nms` 经 `setDetectThresholds` 生效，model_type 由 `det_model_type` 指定，默认 `auto`）。颜色/抛洒物模型路径不用于主检测。
2. **3 个推理线程** `rknn_infer_thread`（`infer_pipeline_threads.cpp:14`）：每核一个，消费共享 `g_infer_queue` → `det.Detect` → 路由到各自 `g_result_queues[camera_id]`。
3. **输入**：每路 `processRadarCamera` 同步喂帧（`infer_processing.cpp:495-507`）→ 推 `g_infer_queue` → `continue`（推理由 rknn 线程异步完成）；SHM 读取线程也推 `g_infer_queue`。
4. **每路 `processResult`**（`infer_processing.cpp:776`）：消费 `g_result_queues` → ByteTrack / 雷达 / 抛洒物（每路 `abandonRecQueue` + 每路 `abandonDetect` 线程，同 Cambricon）/ 发布。**无颜色分类**（`infer.h` 中仅 SOPHON/NVIDIA 有 `vehicle_color_detector`）。
5. **SHM 输入**：独立线程读共享内存 → 推 `g_infer_queue`（`infer_processing.cpp:332`）。

### NVIDIA (USE_NVIDIA) —— 双模式（每路同步 / 全局批量）

- **架构**：基于 TensorRT（`.engine`，`YoloV8_det` 经 `nvinfer` 运行时加载，`detector_nvidia.cpp`）。支持**两种模式**，由配置 `batch_mode` 切换（`config/infer_nvidia.yaml`）。主检测/抛洒物/颜色阈值（`conf_threshold`/`nms_threshold`/`vechile_color_rate`/`abandon_rate`）均生效。
- **ROI 放大增强（仅 NVIDIA）**：按相机在 `camera_roi` 单独配置（`enabled` + `height_ratio`/`width_ratio`/`x_ratio`/`y_ratio`），未配置的相机 ROI 关闭；`nms_iou_threshold` 用于主图+ROI 结果合并去重（`nvidia_pipeline.cpp:17`）。

**模式一：每路同步（`batch_mode=false`，默认）**
- 每路独立 `Detector detector` / `Detector abandon_detector` / `VehicleColorDetector vehicle_color_detector`（`infer.h`）。
- `processRadarCamera` 同步流程（`infer_processing.cpp:521` 以下非 batch 分支）：`detector.inference(mat_receive)` → 绘制检测框 → 抛洒物（按 `abandon_rate` 跳帧 → `abandonRecQueue` → 每路 `abandonDetect` 线程）→ 雷达投影 → `bytetrack.update` → **颜色分类**（crop → `vehicleColorQueue` → 每路 `vehicleColor` 线程）→ 发布。
- 有颜色分类（每路独立 worker）。**无 SHM**（同步模式不启动 SHM 读取线程）。

**模式二：全局批量（`batch_mode=true`，commit `405fc60` 新增）**
- 全局 `NvidiaPipeline`（单个 `YoloV8_det` engine，所有相机共享，`infer_nodelet.cpp:654-686`）；批内可混入主图帧与 ROI 放大图槽位。
- **三段式流水线**（同 Sophon）：
  1. `batch_preprocess_thread`（`infer_pipeline_threads.cpp:48`）：round-robin 扫描各相机 `g_cam_frame_queues`，每路最多取 1 帧、多次扫描凑满 `max_full`（上限 `batch_size`；ROI 开时钳制 ≤ batch_size/2）→ `preprocessAndForward`（`pre_process`→`forward`→`export_batch_data`，`nvidia_pipeline.cpp:89`）
  2. `batch_postprocess_thread`：`postProcess`（`nvidia_pipeline.cpp:152`，用随队列传递的 `nvidia_pparams`/`nvidia_output`，可并行）→ 解码 → ROI 槽位坐标映射回原图并入父相机 → `nmsMerge` 去重 → 按 camera_id 路由到 `g_result_queues`
  3. **每路 `processResult`**（`infer_processing.cpp:776`）：消费 `g_result_queues[camera_id_]` → ByteTrack → 雷达 → 颜色（crop → `vehicleColorQueue` → 每路 `vehicleColor` 线程）→ 抛洒物（每路 `abandonRecQueue` + `abandonDetect` 线程）→ 发布（`publishThread`）
- **SHM 输入**：batch 模式支持 SHM（`use_shm=true` 时独立线程读共享内存 → 推 `g_cam_frame_queues`，`infer_processing.cpp:359-380`）。

### Cambricon (USE_CAMBRICON) —— 每路同步

- **架构**：每路相机独立推理，不共享全局 pipeline。基于寒武纪 CNRT（`cn::Net`），实现位于 `src/detector.cpp`（`#if USE_CAMBRICON` 分支，无独立目录）。
- **模型**：每路独立加载 `Detector detector`（主检测，`.cambricon`）+ `Detector abandon_detector`（抛洒物，`.cambricon`）。无颜色分类模型（`infer.h:212-216` 颜色仅 SOPHON/NVIDIA 有）。
- **主流程**（`processRadarCamera`，`infer_processing.cpp:306`，`#if !USE_SOPHON && !USE_RKNN` 同步路径）：
  1. `detector.inference(mat_receive)` **同步推理**（`infer_processing.cpp:537`），后处理含 letterbox、置信度过滤（`conf_thred`）、NMS（`nms_thred`）（`detector.cpp:79-189`）
  2. 绘制检测框（`infer_processing.cpp:538-541`）
  3. 抛洒物按 `abandon_rate` 跳帧（`rec_index % abandon_rate == 0`，`infer_processing.cpp:546-551`）→ `abandonRecQueue` → 独立 `abandonDetect` 线程（`infer_auxiliary.cpp:185`）过滤重叠框后 → `abandonResultQueue` → 主循环消费并入结果（`infer_processing.cpp:695-742`）
  4. 雷达投影（`infer_processing.cpp:559-590`）
  5. `bytetrack.update` 跟踪（`infer_processing.cpp:604`）
  6. 发布（`infer_processing.cpp:751-757`）
- **阈值**：主检测应用 YAML `conf_threshold` / `nms_threshold`（`infer_auxiliary.cpp:66` → `detector.cpp:13-16`）；抛洒物检测器未单独配置阈值（用默认值）。
- **ROI**：不支持（`getRoiRect`/`drawRoiBox` 被 `#if USE_NVIDIA` 包裹）。
- **SHM**：配置可读但**无读取线程**，`use_shm=true` 时主循环 `usleep + continue` 空转、无图像（见下方 SHM 注）。

### Ascend (USE_ASCEND) —— 每路同步（双输入模式）

- **架构**：每路同步推理，基于昇腾 ACL + AclLite（DVPP），推理实现在 `src/ascend/detector_ascend.cpp`（`YoloV8_det`）。Ascend 构建同时编译**两个 nodelet**（`nodelet_plugins.xml` 均注册）：通用 `DataNodelet` 与 RTSP 专用 `DataNodeletRtsp`，二者共用 `InferDet::processRadarCamera` 推理流程，仅**输入来源**不同。
  - **模式一（订阅 ROS 图像，同 Cambricon）**：通用 `DataNodelet`（`infer_nodelet.cpp:23`），由 `receive_img_topic` 订阅图像 → `imgCallback` → `imgQueue`。launch 默认启用此模式（`infer.launch:7`）。
  - **模式二（RTSP 输入）**：`DataNodeletRtsp`（`infer_ascend_rtsp.cpp:33`），由 `rtsp/enabled` 开关控制（`infer_ascend_rtsp.cpp:237-242`）；每路独立 `rtspReadThread`（`infer_ascend_rtsp.cpp:179`）用 `AclLiteVideoProc` 读 RTSP 流 → DVPP 解码 + `CopyImageToLocal` → NV12→BGR → 推入 `imgQueue[thread_idx]`（`infer_ascend_rtsp.cpp:216-223`）。
- **设备分配**：多路按 round-robin 分配到检测到的昇腾设备 `/dev/davinci*`（RTSP 模式 `infer_ascend_rtsp.cpp:281-294`；ROS 模式 `infer_nodelet.cpp:569-579`）。
- **模型**：每路加载 `det_engine_name`（主检测，`.om`）+ `abandon_engine_name`（抛洒物，`.om`）。`color_engine_name`/`freid_engine_name` 仅读路径但**不使用** → **无颜色分类**。
- **主流程**：`processRadarCamera`（`infer_processing.cpp:306`）同步路径：`Detector::inference` → `yolo_det.Detect` 单图推理（`detector.cpp:252`，AclLite 预处理 + ACL 推理 + NMS）→ 抛洒物按 `abandon_rate` 跳帧 → `abandonDetect` 线程 → 雷达投影 → `bytetrack.update` → 发布。
- **阈值**（**依输入模式而异**）：
  - **ROS 模式**（通用 `DataNodelet`）：调用 `setDetectThresholds`（`infer_nodelet.cpp:126`）→ `detector.cpp:245-247`，YAML `conf_threshold`/`nms_threshold` **生效**（默认 conf=0.35 / nms=0.45，`infer_nodelet.cpp:483-485`）。
  - **RTSP 模式**（`DataNodeletRtsp`）：**未调用** `setDetectThresholds`，使用 `YoloV8_det` 头文件默认值 conf=0.35 / nms=0.45（`detector_ascend.hpp:90-91`），YAML 阈值**不生效**（死配置）。
- **traffic_analyzer**：Ascend 特有支持（`infer_ascend_rtsp.cpp:117-158`、`infer_nodelet.cpp:134-178`），由 `traffic_analyzer/enabled` 启用，含模板加载 / 学习。
- **ROI**：不支持（`getRoiRect`/`drawRoiBox` 被 `#if USE_NVIDIA` 包裹）。
- **SHM**：落在 `#else` 分支，**无 SHM 读取线程**，同 Cambricon（见下方 SHM 注）。
